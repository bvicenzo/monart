include: package:lints/recommended.yaml

formatter:
  page_width: 120

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  rules:
    # Style
    always_declare_return_types: true
    require_trailing_commas: true
    prefer_single_quotes: true
    directives_ordering: true
    sort_constructors_first: true

    # Design
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_final_in_for_each: true
    use_super_parameters: true

    # Explicit over implicit (code-as-prose)
    # Named parameters over positional booleans — intent is visible at the call site
    avoid_positional_boolean_parameters: true
    # Required named parameters listed first — most important constraints are visible first
    always_put_required_named_parameters_first: true

    # Named over anonymous (code-as-prose: extracted identifiers give boundaries)
    # Prefer tearoffs and named functions over lambdas that simply forward a call
    unnecessary_lambdas: true
    avoid_function_literals_in_foreach_calls: true

    # Immutability
    prefer_const_constructors: true
    prefer_const_declarations: true

    # Safety
    avoid_dynamic_calls: true
    avoid_returning_null_for_void: true
    cancel_subscriptions: true
    close_sinks: true
    literal_only_boolean_expressions: true
    no_adjacent_strings_in_list: true
    test_types_in_equals: true
    throw_in_finally: true
    unnecessary_statements: true

    # Error handling (code-as-prose: explicit over implicit)
    # Catch only what you expect — bare catch silently swallows programming errors
    avoid_catches_without_on_clauses: true
    # throw is reserved for Error and Exception subclasses
    only_throw_errors: true

    # Documentation
    public_member_api_docs: true
